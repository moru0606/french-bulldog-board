<h1>index</h1>
  <% @posts.each do |post| %>
    <p><%= post.title %></p>
    <%= post.content %>
    <%= link_to '削除',post_path(post.id), class: 'btn btn-outline-danger', method: :delete, data: { confirm: '  本当に削除してよろしいですか？'} %>
  <% end %>
